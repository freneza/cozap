{
  "name": "cozap",
  "private": true,
  "version": "0.1.0",
  "description": "Plataforma de comunicação descentralizada para comunidade Alumni Poli",
  "packageManager": "pnpm@9.15.4",
  "engines": {
    "node": ">=22.0.0",
    "pnpm": ">=9.15.0"
  },
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "clean": "turbo run clean && rm -rf node_modules"
  },
  "devDependencies": {
    "prettier": "^3.3.0",
    "turbo": "^2.8.20",
    "typescript": "^5.5.0"
  }
}
